<Window x:Class="TaskManager.UI.Views.AddEditTask"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
       Title="Add Task"
       Height="300" Width="400"
       WindowStartupLocation="CenterOwner"
       ResizeMode="NoResize"
       md:DialogServiceViews.IsRegistered="True">
    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="Title" Value="Add Task"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                    <Setter Property="Title" Value="Edit Task"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Style>
    <Grid Margin="20">
       <Grid.RowDefinitions>
           <RowDefinition Height="Auto"/>
           <RowDefinition Height="Auto"/>
           <RowDefinition Height="Auto"/>
           <RowDefinition Height="*"/>
           <RowDefinition Height="Auto"/>
       </Grid.RowDefinitions>

       <TextBlock Text="Title:" Grid.Row="0" VerticalAlignment="Center"/>
       <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Margin="80,0,0,0" Height="25"/>

       <TextBlock Text="Description:" Grid.Row="1" VerticalAlignment="Center" Margin="0,10,0,0"/>
       <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Margin="80,10,0,0" Height="60" AcceptsReturn="True"/>

       <CheckBox Content="Completed" IsChecked="{Binding IsCompleted}" Grid.Row="2" Margin="80,10,0,0" VerticalAlignment="Center"/>

       <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
           <Button Content="OK" Width="75" Margin="0,0,10,0"
                   IsDefault="True"
                   Command="{Binding OkCommand}"/>
           <Button Content="Cancel" Width="75"
                   IsCancel="True"
                   Command="{Binding CancelCommand}"/>
       </StackPanel>
   </Grid>
</Window>